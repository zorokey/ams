<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="emsd.ams.controller.App"
    displayBlock="true">
    <App
        id="app"
        busy="{appView>/busy}"
        busyIndicatorDelay="{appView>/delay}"/>
</mvc:View>